---
name: feature-requirements-matrix
description: 为软件工程 Feature、新特性、版本需求或功能改造编写需求矩阵/排期矩阵/开发工作量矩阵。用户要求“拆任务”“估人天”“工作量评估”“开发排期”“多人排期”“开发计划”“需求矩阵”“甘特图”时使用本 skill；不要因为 Android、后端、Web、iOS、桌面端、云平台等领域不同而选择不同 skill。触发后先判断项目类型：若上下文能确定是 Android App、AOSP/系统应用、APK/AAR、Gradle Android Library、Android SDK/Framework、Kotlin/Java Android 或 Android 端侧模块，读取 references/android-template.md；若能确定是后端、Web、iOS、桌面端、云平台、数据平台、AI/ML 服务、基础设施、中台系统、非 Android SDK/库、CLI 或企业内部系统，读取 references/common-template.md。若用户只说 App/客户端/移动端/端侧/SDK/模板而无法判断项目类型，先追问项目类型再选模板。若用户要求技术方案/技术设计/实现方案，使用 feature-technical-design；若要求概要设计/HLD，使用 hld-generator。
---

# Feature 需求矩阵生成器

## 目标

根据用户提供的原始需求资料和技术方案，为软件工程 Feature、新特性、版本需求或功能改造生成一份可评审、可排期、可执行的需求矩阵。矩阵覆盖功能拆解、依赖关系、工作量、人力安排、开始/完成时间、负责人、适用端/平台或设备、风险和状态，并在多人开发时给出任务甘特图。

本 skill 不在触发层区分 Android 与通用工程；只按“文档类型 = 需求矩阵/排期/任务拆解”触发。触发后再选择领域模板。

## 触发场景

使用本 skill 处理以下任务：

- 用户要求拆任务、拆开发任务、拆研发任务。
- 用户要求估人天、评估工作量、给出开发计划。
- 用户要求按多人并行安排、排期、输出甘特图。
- 用户要求写需求矩阵、排期矩阵、开发任务矩阵、开发工作量矩阵。
- 用户要求基于 PRD、技术方案或接口设计拆解任务。

不要使用本 skill 处理：

- 技术方案、技术设计、实现方案、方案评审材料：使用 `feature-technical-design`。
- 概要设计、HLD、高层设计、公司概要设计模板：使用 `hld-generator`。
- 纯产品/运营排期，不涉及软件工程开发任务。

## 项目类型判定与模板选择

### 1. Android 项目

如果用户资料、工程路径、术语或上下文明确出现以下信息，选择 Android 模板：

- Android App、AOSP、系统应用、端侧 Android 模块。
- APK、AAR、Gradle Android Library、AndroidManifest、minSdk、targetSdk。
- Android SDK、Android Framework、Framework API、系统服务、AIDL、Intent、Broadcast、ContentProvider。
- Kotlin/Java Android、Activity、Fragment、ViewModel、Repository、Service、Receiver、Provider、Worker、Compose/XML。

读取：`references/android-template.md`。

### 2. 通用非 Android 软件工程

如果用户资料、工程路径、术语或上下文明确属于以下项目，选择通用模板：

- 后端服务、Web 前端、iOS、桌面端、云平台、数据平台、AI/ML 服务、基础设施、中台系统。
- 非 Android SDK/库、CLI 工具、企业内部系统、数据管道、服务端批处理。
- HTTP/RPC/GraphQL/WebSocket、API 网关、数据库、缓存、消息队列、对象存储、CI/CD、部署、灰度、监控告警。

读取：`references/common-template.md`。

### 3. 无法判断项目类型

如果用户只说“App”“客户端”“移动端”“端侧”“SDK”“Framework”“按模板”，但没有明确 Android 或非 Android 工程类型，不要直接生成完整矩阵。先追问：

> 这个项目属于 Android 工程，还是后端/Web/iOS/桌面端/云平台等非 Android 工程？我需要先确认项目类型，才能选择正确需求矩阵模板。

如果用户回答能判断为 Android，使用 Android 模板；否则使用通用模板。

## 必要输入

开始编写前，先确认以下输入是否齐全：

1. 原始需求资料：PRD、需求说明、需求截图、交互稿、接口说明、Jira/Confluence 链接或用户粘贴的需求内容。
2. 技术方案文档：决定实现细节的方案文档、概要设计、接口/模块设计，或用户提供的等价说明。
3. 开始时间：必填，格式优先使用 `YYYY-MM-DD`。
4. 结束时间：选填。
5. 参与该需求开发的人员数量：必填。

如果缺少原始需求资料、技术方案、开始时间或开发人数，不要直接编写完整矩阵；先让用户补充缺失信息。结束时间缺失时可以继续，但需要明确说明会按任务量自动编排。

## 工作流程

### 1. 认真阅读资料

先阅读用户提供的所有资料和文档。不要只根据标题、文件名或模板示例生成矩阵。

- 如果用户给的是本地文件路径，使用文件工具读取内容。
- 如果用户给的是工程路径或代码上下文，先搜索关键模块、入口、调用链和已有实现。
- 如果资料很长，先提炼需求范围、业务流程、技术实现路径、外部依赖和待确认点。
- 对无法从资料中确定的信息，用“待确认”标注，不要编造。

### 2. 读取对应模板

根据“项目类型判定与模板选择”读取其中一个模板：

- Android：`references/android-template.md`
- 通用非 Android：`references/common-template.md`

生成矩阵时保持所选模板的项目基本信息、需求矩阵必填列和新功能必写行，不要删除必填内容。

### 3. 拆解任务

先按依赖关系拆分，再估算工作量：

- 每个需求矩阵行代表一个可交付子任务，粒度控制在 3 人天以内。
- 如果某项超过 3 人天，继续拆分到 3 天以内。
- 被依赖任务排在前面；依赖关系写入“前置依赖条件”。
- 不要把评审、沟通、联调、冒烟、自测全部混成一个大任务，必要时拆成独立行。
- 工作量以开发人天估算，可以包含开发、自测、联调和必要文档，但不要把 QA 专项测试算入开发工作量，除非用户明确要求。

### 4. 排期规则

- 日期默认按工作日编排，只安排周一到周五。
- 如果开始时间落在周末，顺延到下一个周一并说明。
- 若有结束时间，在结束时间前预留 2 个工作日缓冲。
- 若无结束时间，按任务总量、依赖、人数和可并行性自动推导完成时间。
- 多人开发时，只并行真正可并行的任务。
- 任务量无法在给定结束时间前完成时，直接说明排期风险，并给出压缩建议。

### 5. 多人开发甘特图

当开发人数大于 1 时，需求矩阵后必须单独给出任务甘特图。优先使用 Mermaid，并与需求矩阵中的开始/完成时间保持一致。

## 输出格式

默认输出完整 Markdown 文档。若用户指定保存路径，则写入该路径，并在完成后说明绝对路径。

## 质量检查清单

输出前检查：

- 是否先确认并选择了 Android 或通用模板。
- 是否已经阅读并理解原始需求资料和技术方案。
- 缺少开始时间、开发人数、原始需求资料或技术方案时，是否先向用户补充询问。
- 每个任务是否都在 3 人天以内。
- 多人开发时，是否只并行真正可并行的任务，并给出甘特图。
- 排期是否默认只使用工作日，且任务开始/完成时间没有落在周六或周日。
- 如果有结束时间，是否在结束时间前预留 2 个工作日缓冲。
- 是否保留所选模板的新功能必写行。
- 是否标注了排期风险、待确认事项和压缩建议。
